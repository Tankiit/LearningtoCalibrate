# AISTATS 2027 — P1 ledger

This is the P1/AISTATS ledger. It contains no P2/ICLR numbers. P2 results are
cited from `../p2_iclr/iclr_2027.md` when needed; they are never copied here.

## Current scope

P1's evaluated functionals are MSCE, group-conditional coverage variance, and
spread. These are coverage objects, not across-item correctness-association
metrics. The P2 Failure 1 control therefore does not apply to P1 evaluation.

## Open provenance question

The Mondrian partition-forming variable has not been identified in this
checkout. Before claiming that P1 is untouched by P2's report-vocabulary
result, record whether the bins are formed from a model-internal feature,
another internal score, or verbalized confidence. If the partition variable
is internal, P1 remains separate; if it is verbalized confidence, the P2
identification result must be cited rather than duplicated.

## Source status

The P1 paper source, preregistrations, and experiment files are not present in
this P2 checkout. Do not add P1 experiment results here until the collaborator
works in a P1 checkout.

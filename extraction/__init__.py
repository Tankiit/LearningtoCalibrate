"""Hidden-state + logprob extraction pipeline.

`modal_app.py` defines the remote functions that run on Modal GPUs.
`download.py` mirrors the resulting Modal-volume artifacts into local outputs/.
"""
